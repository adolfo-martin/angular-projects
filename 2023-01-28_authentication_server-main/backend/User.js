export default class User {
    constructor(uuid, username, firstname, lastname, password) {
        this.uuid = uuid;
        this.username = username;
        this.firstname = firstname;
        this.lastname = lastname;
        this.password = password;
    }
}